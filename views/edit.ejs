<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="min-h-screen bg-zinc-900 text-white w-full p-10">
        <a class=" bg-red-500 rounded-md px-2 py-1 text-sm mb-5 inline-block" href="/logout">Logout</a>
        <h5 class="mb-5 ">Edit your post.</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="Whats on your mind ?" class="p-3 outline-none w-1/3 block resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%= post.content %></textarea>
            <input type="submit" value="Submit Changes" class="px-3 py-2 text-sm bg-yellow-500 block text-black rounded-md mt-2 w-40">
        </form>
    </div>
  </body>
</html>